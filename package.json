{
  "name": "blind-shot",
  "version": "0.1.0",
  "private": true,
  "description": "Blind Shot — throw a dart at an offscreen target using X/Y edge indicators",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "playwright": "^1.61.1",
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  }
}
